# Wordpress

```
# Dupliquer le ficher avec les variable par défault
cp .env.sample .env

# Editer le en modifiant les valeurs

# Lancer l’application
docker-compose run -d

# Lister les conteneurs
docker-compose ps

# Appeller la page
curl localhost:8000

# Arreter l'application et supprimer les conteneurs
docker-compose down
```
